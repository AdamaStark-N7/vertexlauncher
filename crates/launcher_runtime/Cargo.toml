[package]
name = "launcher_runtime"
version = "0.1.21-Alpha"
edition = "2024"

[dependencies]
runtime_bootstrap.workspace = true
tokio.workspace = true
tracing.workspace = true

[target.'cfg(all(target_os = "linux", target_env = "gnu"))'.dependencies]
libc = "0.2"
